node_modules
dist
# raw originals contain GPS EXIF - never commit
photos/
scripts/.geocache.json
*.local
# photo pool: served images + answers (GPS/place/date), regenerated by `npm run pool`
public/pool/
server/data/
